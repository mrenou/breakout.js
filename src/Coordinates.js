define("Coordinates", function() {
        console.log("Coordinates loading...");
        function Coordinates(x, y) {
            this.x = x;
            this.y = y;
            return this;
        }
        return Coordinates;
    }
);
