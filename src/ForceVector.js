define("ForceVector", function() {
        console.log("ForceVector loading...");
        function ForceVector(x, y) {
            this.x = x;
            this.y = y;
            return this;
        }
        return ForceVector;
    }
);